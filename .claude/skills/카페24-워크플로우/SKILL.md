---
name: 카페24-워크플로우
description: 카페24 표준 워크플로우 시작·진행·재개 — 6단계 자동 진행, 단계 게이트, 자동 검증, 재진입까지 관리
---

카페24 워크플로우를 시작·진행·재개합니다. `카페24-워크플로우` 에이전트를 호출하세요.

진입 시나리오:

### A. 새 워크플로우 시작
사용자가 처음 진입한 경우:

1. 다음 질문 (한 번에 모두):
   - 어떤 작업이세요? (한 줄 설명)
   - 어떤 클라이언트?
   - 어떤 워크플로우 쓰시겠어요?
     - **09-full-renewal** — ★A-to-Z 전체 페이지 개편 (유형별 재마크업/재스타일/JS재구축 자동 배분) `[검증됨·프로토타입]`
     - **08-ez-three-step-pingpong** — 레거시 EZ 3단계 핑퐁 (HTML skin → EZ FTP → strip 스킵/선별)
     - **07-ez-on-legacy-setup** — 레거시+EZ 초기 세팅 전체 (Phase 0~4)
     - **05-reference-intake** — 구현 전 인입 (`/레퍼런스인입`, URL 또는 시안)
     - **04-measure-first** — 수정 전 실측 (`/요소측정` 선행)
     - **01-quick-fix** (3단계, 20~60min) — 단일 변경
     - **02-skin-build-standard** (6단계, 1~3일) — 큰 작업
     - **03-reference-renewal** (8단계, 3~7일) — 레퍼런스/시안 1:1 (`05` 승인 후)
2. 답 받으면 — **환경 없으면 `/접속세팅` 먼저** — `clients/{클라}/.workflow.md` 생성
3. 해당 워크플로우 정의 파일 (`01_작업하기/workflows/{번호}-{이름}.md`) 읽어서 1단계 시작
4. 메인 에이전트(루트 `CLAUDE.md`)에 1단계 본문 위임

### B. 진행 중인 워크플로우 재개
사용자 폴더에 `.workflow.md` 이미 있는 경우:

1. `.workflow.md` 읽음
2. 마지막 단계 상태 보고 후 이어서 진행 여부 확인
3. Y → 해당 단계 이어가기 (메인 에이전트 위임)
4. N → 사용자 원하는 단계로 (게이트 조건 확인)

### C. 상태 조회만
사용자가 "지금 어디까지 했어?" 같은 질문:

1. `.workflow.md` 읽고 표 형태로 진행 상황 보여드림
2. 다음 단계 제안

---

## 핵심 규칙

- **단계 건너뛰기 절대 금지** — 사용자가 요청해도 1·2차 거부
- **각 단계 자동 검증** — 단계 정의 파일의 `자동 검증` 에이전트 호출
- **검증 실패 시 자동 재진입** — 이전 단계로 복귀, 실패 부분만 재작업
- **사용자 컨펌 게이트** — 단계 완료 시 사용자 컨펌 필수
- **상태 영속** — `.workflow.md` 에 모든 단계·검증·재진입 기록

## 직접 코드 작업 안 함

코드 본문은 메인 에이전트(루트 `CLAUDE.md`)에 위임. 이 에이전트는 **지휘·게이트·검증·기록**만.

## 관련 문서

- 워크플로우 정의: `01_작업하기/workflows/README.md` 부터 시작
- 두뇌 문서: `brain/docs/CAFE24-SMARTDESIGN-AGENT.md`
- 상태 파일 예시: `clients/_template/.workflow.md.example`
