# 예시 데모 몰 (선택 — 연습용)

> 키트를 쓰려면 **이 URL이 필수는 아닙니다.** 본인·클라이언트 몰 ID로 `/접속세팅` 하면 됩니다.  
> 아래는 PLP 실측·score 스크립트 **연습**에 쓰인 공개 데모 몰 예시입니다.

---

## Ref / Tgt (PLP 연습)

| 구분 | URL |
|------|-----|
| **Ref** (레퍼런스) | https://ecudemo393674.cafe24.com/product/list.html?cate_no=24 |
| **Tgt** (작업 대상) | https://ecudemo400786.cafe24.com/product/list.html?cate_no=24 |

```bash
python work/scripts/ref393674-score-plp.py
```

FAIL 항목만 수정 → FTP → 재실측 → 100점까지 반복.

---

## 이 몰이 쓰인 패턴 (HTML 타입 + EZ FTP 오버레이)

> **ecudemo400786:** 관리자 `editor_type=HTML` (Easy 타입 **아님**). EZ skin16 **코드**를 FTP로 `/sde_design/base/`에 엎어쓴 뒤 `_ref393674/` 커스텀 레이어만 추가 — **F35 권장 패턴**.

- **관리자 타입:** 스마트디자인(HTML) — Easy 등록·대표 전환 없음
- **Base (FTP):** EZ skin16 파일 선별 이식 → `/sde_design/base/`
- **커스텀:** `_ref393674/` 레이어 (layout 링크만 추가, skin1·EZ core 미수정)
- **초기 세팅 워크플로:** [`01_작업하기/workflows/07-ez-on-legacy-setup.md`](../01_작업하기/workflows/07-ez-on-legacy-setup.md) Phase 0-D

## 언제 보면 되나

- 레퍼런스 1:1·verify-loop 흐름을 **처음 익힐 때** (본인 몰 없이 따라 해보기)
- **레거시 몰 + EZ 엎어쓰기** 접근이 맞는지 판단·연습할 때
- 강의·워크숍에서 강사가 같은 URL을 쓰는 경우 (키트 배포와 무관한 **선택** 자료)

본인 프로젝트가 있으면 Ref/Tgt를 **자기 레퍼런스·타겟 몰**로 바꿔 `/레퍼런스인입` 하세요.
